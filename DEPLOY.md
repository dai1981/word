# ロリポップへのアップロード設定

**指定したバッチのページだけ**をロリポップへ送ります。
過去のバッチには一切触れません。更新もしませんし、削除も起こりません。

---

## 1. ロリポップのFTP情報を確認

ロリポップのユーザー専用ページ →「ユーザー設定」→「アカウント情報」で次を確認:

- FTPサーバー（例: `ftp.lolipop.jp` など、表示されたホスト名）
- FTPアカウント（FTP・WebDAVアカウント）
- FTPパスワード（FTP・WebDAVパスワード）

## 2. 保存先パスを確認

`.github/workflows/deploy.yml` の `cd /word/word;` が、実サイトで
`https://www.eigo-duke.com/word/word/` に対応するサーバー上の場所と
一致しているか確認してください。多くの場合このままでOKです。

## 3. 実行

GitHub の **Actions タブ → 「Deploy to Lolipop」→ 右上の「Run workflow」**

| 入力欄 | 内容 |
|---|---|
| `batch` | 送りたいバッチ名。例: `missing-batch1`（拡張子不要）<br>複数まとめるならカンマ区切り: `missing-batch1,missing-batch2` |
| `dry_run` | チェックを入れると、転送せず対象ファイル一覧だけ表示 |

**main に push しても自動実行はされません。** コミットは自由に重ねて、
本番へ出したいタイミングで手動実行する運用です。

初めてのバッチは `dry_run` にチェックを入れて実行し、想定どおりの
ファイルだけが並んでいるか確認してから本番を回すと安全です。

---

## 動作の仕組み

ワークフローは2段階で動きます。

```
1. python3 generate.py --batch missing-batch1 --out _deploy
     → data/missing-batch1.json だけを読み、_deploy/ に15ページだけ生成
2. lftp の mput で _deploy/*.html を /word/word/ へアップロード
```

`--batch` で指定した JSON 以外は**読み込みすらしません**。
`word/word/` にある過去バッチのHTMLも参照しません。

転送に使う `mput` はアップロード専用のコマンドで、削除機能を持ちません。
サーバー上のファイルが消えることは構造的に起こり得ません。
同名ファイルがあれば上書き（＝更新）されます。

以前使っていた `SamKirkland/FTP-Deploy-Action` は「ディレクトリ同期」で、
ローカルに無いファイルをサーバーから削除する動作を含んでいました。
状態ファイル（`.ftp-deploy-*.json`）も不要になったので、サーバー上に
残っていれば削除して構いません。

### 安全弁

- 転送対象が0件なら中止します（生成失敗時に空ディレクトリを送らないため）
- 転送対象が500件を超えたら中止します（バッチ名の指定ミス防止）
- 存在しないバッチ名を指定すると `generate.py` がエラーで停止します

---

## リポジトリ側の運用

`data/*.json` は追記式で、過去バッチは消さないでください。
デプロイ対象からは外れますが、**次に作る単語を決めるときの重複チェックの基準**になります。

`word/word/` にコミットされたHTMLは、リポジトリ上の記録として残ります。
デプロイには使いませんが、`generate.py` を全件実行したときの出力先なので、
そのままにしておいて構いません。

---

## 注意

- `ftp:ssl-force true`（FTPS）で接続に失敗する場合は、`deploy.yml` の
  `set ftp:ssl-force true;` を `set ftp:ssl-force false;` に変えて再実行してください
- 初回は1バッチで試し、ロリポップ上で正しく表示されるか確認してから本格運用を
- 実行後は Actions のログでアップロードされたファイル名を確認する習慣をつけてください
- `generate.py` のテンプレートを変更した場合、**過去バッチのページは自動では
  更新されません。** 全ページに反映したいときは
  `python3 generate.py`（引数なし）でローカル生成し、FTPクライアントから
  手動でアップロードしてください
