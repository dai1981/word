# ロリポップへ「ワンボタン」アップロード設定

GitHub の `word/word/` を、ボタン1つでロリポップへ一括アップロードします。
FTPのパスワードは GitHub の Secrets に保存され、コードや会話には残りません。

---

## 1. ロリポップのFTP情報を確認

ロリポップのユーザー専用ページ →「ユーザー設定」→「アカウント情報」で次を確認:

- FTPサーバー（例: `ftp.lolipop.jp` など、表示されたホスト名）
- FTPアカウント（FTP・WebDAVアカウント）
- FTPパスワード（FTP・WebDAVパスワード）

## 2. 保存先パスを確認

`.github/workflows/deploy.yml` の `server-dir: /word/word/` が、実サイトで
`https://www.eigo-duke.com/word/word/` に対応するサーバー上の場所と一致しているか確認。
違う場合だけ書き換えてください（多くの場合このままでOK）。

## 3. 実行（ワンボタン・手動のみ）

GitHub の **Actions タブ → 「Deploy to Lolipop (s-)」→ 右上の「Run workflow」** を押すだけ。
`word/word/` のHTMLがロリポップへアップロードされます。

**main に push しても自動実行はされません。** `deploy.yml` の `on:` から `push:` を
外してあるためです。コミットは自由に重ねて、本番へ出したいタイミングで手動実行する運用です。
何バッチか溜めてからまとめて1回流して構いません。

---

## 動作の仕組み（重要）

`SamKirkland/FTP-Deploy-Action` を使っています。これは単純なアップロードではなく
**同期**で、サーバー上の状態ファイル（`state-name` で指定、例: `.ftp-deploy-s.json`）に
記録された「前回このワークフローがアップロードしたファイル一覧」と比較して差分を転送します。

- 新規ファイル → アップロード
- 内容が変わったファイル → 上書き
- **状態ファイルに載っていて、今回リポジトリに無いファイル → サーバーから削除**

削除対象になるのは3つ目のケースだけです。FTPクライアントで直接置いたファイルや、
このワークフローが一度も触っていない既存ページは状態ファイルに載っていないため、
削除されることはありません。

### だから `data/*.json` は消さないこと

`generate.py` は `data/*.json` から毎回すべてのページを再生成します。
過去バッチの JSON を削除すると、そのページが `word/word/` から消え、
同期によってサーバー側からも削除されます。**追記式で運用すれば常に全ページが揃います。**

### 複数ワークフローを併用する場合

`Deploy to Lolipop (s-)` と `Deploy to Lolipop (r-)` のように分けている場合、
`state-name` を必ず別名にしてください（`.ftp-deploy-s.json` / `.ftp-deploy-r.json`）。
同じ状態ファイルを共有すると、一方が他方のアップロードしたファイルを
削除対象と判断します。

あわせて、**どちらのワークフローからも `push:` を外してください。**
片方に残っていると、コミットのたびに両方が起動して同時に同期が走ります。

---

## 注意

- `protocol: ftps` で失敗する場合は `ftp` に変更して再実行。
- 初回は数ページで試し、ロリポップ上で正しく表示されるか確認してから本格運用を。
- `generate.py` を変更した直後は、既存ページも含めて全ファイルの内容が変わるため、
  次の1回だけ `word/word/` の全ファイルが転送されます。時間がかかりますが正常です。
- 実行後は Actions のログでアップロード数・削除数を確認する習慣をつけると安全です。
